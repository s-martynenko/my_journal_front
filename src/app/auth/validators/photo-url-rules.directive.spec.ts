import { PhotoUrlRulesDirective } from './photo-url-rules.directive';

describe('PhotoUrlRulesDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoUrlRulesDirective();
    expect(directive).toBeTruthy();
  });
});
